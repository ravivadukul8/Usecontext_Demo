import React from 'react';
import { Actions } from '../actions/user';
export declare type User = {
    id: string;
    name: string;
    loginCount: number;
};
export declare const userReducerArgs: {
    reducer: React.Reducer<User, Actions>;
    initialState: User;
};
