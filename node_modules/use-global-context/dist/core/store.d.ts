import { GlobalContextReducers, State, GlobalContextValue, PartialState } from '../createGlobalContext/type';
import { Subscription } from './subscription';
export declare class Store<R extends GlobalContextReducers> {
    private reducers;
    private state;
    private dispatch;
    private subscription;
    constructor(reducers: R, subscription: Subscription<GlobalContextValue<R>>);
    getStore: () => GlobalContextValue<R>;
    setState: (state: PartialState<R>) => void;
    setPartialState: (partial: keyof State<R>, value: State<R>[keyof State<R>]) => State<R>;
    getSubscription: () => Subscription<any>;
    trySubscribe: () => void;
    reset: (subscription: Subscription<GlobalContextValue<R>>) => void;
}
