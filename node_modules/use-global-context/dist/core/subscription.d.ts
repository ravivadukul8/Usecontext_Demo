export declare class Subscription<S> {
    private listeners;
    constructor();
    trySubscribe: (store: S) => void;
    addListener: (listener: (state: S) => void) => void;
    deleteListener: (listener: (state: S) => void) => void;
    reset: () => void;
}
